{
  "name": "pecunia-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end tests (Playwright) for the Pecunia application.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^24.0.0"
  }
}
